import math   #biblioteca de matemática
num=int(input('Digite um número:'))
raiz = math.sqrt(num)  #funcionalidade de raiz quadrada
print('A raíz de {} é igual a {}'.format(num,math.ceil(raiz)))  #retorna a raiz quadrada arredondada para cima
